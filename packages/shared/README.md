# shared

Tipos e contratos TypeScript compartilhados entre `backend`, `worker` e `business`.

Fica vazio de propósito até existir um segundo consumidor real do mesmo tipo — evita abstração prematura.
